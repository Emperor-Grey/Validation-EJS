<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
    />
    <link rel="stylesheet" href="../public/loginStyle.css" />
    <title>Login Page</title>
  </head>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');

    :root {
      --main-color: #3a0ca3;
      --background-color: #3f68e2;
      --light-background: #add8e6;
      --gradient-background: linear-gradient(
        to top,
        var(--light-background),
        #87ceeb
      );
      --form-background: lightblue;
      --box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
      --border-radius: 20px;
      --input-background: rgba(255, 255, 255, 0.5);
      --button-background: rgba(255, 255, 255, 0.5);
      --hover-background: hsla(0, 0%, 96%, 0.5);
      --social-background: rgba(255, 255, 255, 0.27);
      --social-hover-background: rgba(255, 255, 255, 0.47);
      --text-color: #2d2d2d;
      --heading-font: 'Inter', sans-serif;
    }

    * {
      padding: 0;
      margin: 0;
      box-sizing: border-box;
      font-family: var(--heading-font);
    }

    body {
      display: grid;
      place-items: center;
      height: 100vh;
      position: relative;
      background: var(--gradient-background);
    }

    .container {
      width: 100vh;
      height: 60vh;
      background: var(--form-background);
      display: flex;
      margin: 20px;
      justify-content: space-around;
      align-items: center;
      box-shadow: var(--box-shadow);
      border-radius: var(--border-radius);
    }

    .image-container {
      width: 100%;
    }

    .container img {
      width: 90%;
      margin-left: 30px;
    }

    .content-container {
      height: 100%;
      width: 70%;
      display: flex;
      flex-direction: column;
      margin-top: 10%;
    }

    .content-heading {
      font-family: var(--heading-font);
      padding: 20px 10px 10px 0px;
      font-style: bold;
      font-size: 35px;
      font-weight: 600;
    }

    .content-subtext {
      font-size: 16px;
      color: var(--text-color);
      padding-bottom: 7px;
      letter-spacing: 0.5px;
      outline: none;
      border: none;
    }

    .signup-text {
      margin-left: 2px;
      text-decoration: none;
      color: var(--background-color);
    }

    .signup-text:hover {
      text-decoration: underline;
    }

    .form-label {
      display: block;
      margin-top: 13px;
      font-size: 16px;
      font-weight: 500;
    }

    .form-input {
      display: block;
      height: 45px;
      width: 90%;
      border-radius: 8px;
      background-color: var(--input-background);
      padding: 0 10px;
      margin-top: 8px;
      border: none;
      outline: none;
      font-size: 14px;
      font-weight: 300;
    }

    .form-input:hover {
      border: 1px solid var(--form-background);
    }

    .content-container button {
      padding: 15px;
      margin-top: 10px;
      background-color: var(--button-background);
      border-radius: 8px;
      border: none;
      outline: none;
    }

    .content-container button:hover {
      background-color: var(--hover-background);
    }

    .submit-button {
      width: 100%;
      padding: 20px 40px 20px 0px;
      overflow: hidden;
    }

    .social {
      display: flex;
    }

    .social div {
      background: var(--main-color);
      width: 140px;
      padding: 10px;
      margin-top: 10px;
      background-color: var(--social-background);
      color: var(--text-color);
      text-align: center;
      border-radius: 10px;
    }

    .social div:nth-child(2) {
      color: blue;
    }

    .social div:nth-child(1) {
      color: rgba(234, 68, 53, 0.734);
    }

    .social div:hover {
      background-color: var(--social-hover-background);
    }

    .social .fb {
      margin-left: 60px;
      margin-right: 40px;
    }

    .social i {
      margin-right: 10px;
    }

    /* Media Query for smaller devices */
    @media screen and (max-width: 768px) {
      .container {
        height: 43vh;
        max-width: 500px;
        flex-direction: column;
      }

      .container img {
        display: none;
      }
    }
  </style>
  <body>
    <main class="container">
      <div class="image-container">
        <img src="../assets/sign.png" alt="signUp image" />
      </div>
      <form class="content-container" action="./login" method="post">
        <h1 class="content-heading">Login</h1>
        <p class="content-subtext">
          Doesn't have an account yet?<a class="signup-text" href="/register"
            >Sign Up</a
          >
        </p>

        <label for="text" class="form-label">Username</label>
        <input
          type="text"
          name="text"
          id="text"
          class="form-input"
          placeholder="Enter your username"
          required
        />

        <label for="password" class="form-label">Password</label>
        <input
          type="password"
          name="password"
          id="password"
          placeholder="Enter your password"
          required
          class="form-input"
        />

        <div class="submit-button">
          <button type="submit" class="submit-button">LOGIN</button>
        </div>

        <div class="social">
          <div class="go">
            <i class="fab fa-google"></i>
            Google
          </div>
          <div class="fb">
            <i class="fab fa-facebook"></i>
            Facebook
          </div>
        </div>
      </form>
    </main>
  </body>
</html>
